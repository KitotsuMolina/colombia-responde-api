import { Body, Controller, Param, Patch, Post, UseGuards } from '@nestjs/common';import { CreateIncidentDto } from '../incidents/create-incident.dto';import { IncidentsService } from '../incidents/incidents.service';import { AdminGuard } from './admin.guard';import { UpdateIncidentLocationDto } from './update-incident-location.dto'
@Controller('admin') @UseGuards(AdminGuard) export class AdminController {constructor(private readonly incidents:IncidentsService){}@Post('incidents') create(@Body() dto:CreateIncidentDto){return this.incidents.create(dto)}@Patch('incidents/:id/location') updateLocation(@Param('id') id:string,@Body() dto:UpdateIncidentLocationDto){return this.incidents.updateLocation(id,dto)}}
